{
  "name": "@asp/scheduling-profile",
  "version": "0.1.0",
  "description": "Agentic Scheduling Profile — an open MCP profile for appointment booking",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "validate-schemas": "node --loader ts-node/esm scripts/validate-schemas.ts",
    "start:server": "node --loader ts-node/esm reference-server/src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "ajv": "^8.17.0",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.0",
    "zod": "^3.23.0"
  }
}
